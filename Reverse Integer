int reverse(int x) 
{
    int rev = 0;   // declare here

    while (x != 0) 
    {
        int digit = x % 10;

        // overflow check
        if (rev > 214748364 || rev < -214748364)
            return 0;

        rev = rev * 10 + digit;
        x /= 10;
    }

    return rev;
}
